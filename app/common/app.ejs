<!DOCTYPE html>
<html lang='en'>
<head>
  <meta charset='UTF-8'>
  <meta name='viewport' content='width=device-width, initial-scale=1'>
  <meta name='description' content='<%= htmlWebpackPlugin.options.title %>'>
  <meta name='application-name' content='<%= htmlWebpackPlugin.options.title %>'>
  <title><%= htmlWebpackPlugin.options.title %></title>
  <% htmlWebpackPlugin.options.fonts.forEach( (font) => { %>
    <link href='<%- font %>' rel='stylesheet' type='text/css' />
  <% }) %>
  <style>
    body { margin: 0; padding: 0 }
    #app { margin: 0; padding: 0; width: 100%; height: 100%; position: absolute; }
    #overlayMessage {
      position: fixed; bottom: 0; width: 100%; z-index: 9999;
      font-family: sans-serif; font-size: 5em;
      opacity: .25; text-align: center;
      pointer-events: none
    }
  </style>
  <script>
    var siteConfig = <%= htmlWebpackPlugin.options.siteConfig %>
  </script>
</head>
<body>
  <!-- TODO: Better loading screen -->
  <div id='app'><%= htmlWebpackPlugin.options.title %> is now loading...</div>
  <noscript>Javascript is disabled. You must enable Javascript to use this site.</noscript>

  <% if (htmlWebpackPlugin.options.overlayMessage) { %>
    <div id='overlayMessage'><%= htmlWebpackPlugin.options.overlayMessage %></div>
  <% } %>
</html>
